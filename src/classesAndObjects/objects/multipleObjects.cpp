#include <iostream>
using namespace std;

class Student {
    // data members and member functions
};

int main()
{
    Student object1; // 1st object
    Student object2; // 2nd object

    Student object3 = {
        /*
        Initialize data members of object3 with appropriate values
        based on the data types defined in the 'Student' class.
        e.g., value1 same as data_type defined in class,
              value2 same as data_type defined in class,
              value3 same as data_type defined in class,
        ...
        */
    };

    return 0;
}
